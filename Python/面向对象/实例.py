"""
创建对象的步骤：
1.在内存空间中进行分配
2.利用__init__()函数进行初始化

对象是类的实例，实例化的过程就是创建对象的过程
对象的属性也被称为实例属性
对象调用的方法被称为实例方法

每一个对象都有自己的独立内存空间
多个对象的方法只有一份，需要的时候就会进行引用传递到内部


类是一个特殊的对象，叫类对象
在程序运行时类对象在内存中只有一份
"""